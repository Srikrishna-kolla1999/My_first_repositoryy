
CLASS-16
->oka app image undi dani ni run cheyyali antey oka server, a server lo docker install cheyyali.but server lekunda koda containers create cheyyachu vth the help of ecs
->using fargate in ecs we can create serverless containers(serverless mean=aws vadu chusukuntadu a servers ni)
->ECS(elastic container service)= used to create a containers
->ECR(elastic container registry)= used to store images
->1st will store image in ecr and using ecs we can cretae a container
CREATING AN IMAGE N STORING IN ECR
-> for code use https://github.com/sreenadh45/nginx.git
->git clone tho by def ga master branch vastadi ala kakunda specific branch kavali antey
git clone -b branch_name url
CREATING REPOSITORY IN ECR
->search ecr n create a repo-name in repository and click on create.
->goto repo n chk any images available or not
->before pushing images to ecr, we need to create provide authentication to ecs
->to communicate one serv to another serv we need to provide IAM permissions
->in aws serach bar search for IAM>goto roles>create a role>in serv give ec2>next>sel admin access>next>give any role-name>create role
->now attach that role to ec2
    sel ec2 instance>actions>security>modify iam role>sel cretaed role>update iam role
TO PUSH IMAGE TO ECR
->no need to create image, jst we should have source code n dockerfile
->now goto ecr>we can see option view push commands
->now run each cmd one by one in server
->

K8S
CLASS2
minikube=it is a tool used to create a single node cluster
->ee node loney worker,manager components untai
MINIKUBE setup
->fr minikube need 2cpu's,20gb n install docker
->search for minikube setup>under installation sel linux n copy those 2 cmds and run in server
->chk minikube version
->chk status minikube status
->to start a user vth admin access is required. so goto driver pages>docker>run the cmd minikube start --driver=docker
->after running we shd use --force bcz we are running the cmd vtha root admin user
minikube start --driver=docker --force
->now chk the minukube status
->minikube got installed successfully
->to create a pod/service... we shd have kubectl
->to install kubectl
search by kubectl install>copy below cmd in server
curl -LO "https://dl.k8s.io/release/$(curl -L -s https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl"
->now kubectl is file got downloaded into server
->give x permissions
chmod +x kubectl
->move the kubectl file to def path
mv kubectl /usr/local/bin/
->ee cmds anni a offcial web lo untai
CREATING A POD
->we can create a pods in 2 ways
    1.imperative(using cmds)
    2.declarative(using manifest files)
 1.imperative
->to create a pod
    kubectl run pod-name --image=image-name
    kubectl run pod-mypod --image=nginx
oka pod create ayye, a pod lo container create ayye, a container nginx image tho run avutundi
NOTE - pod name shd be in small
->to se list of pods
    kubectl get pods
->to see particular pod
    kubectl get pods pod-name
->to get some more details of a pod
    kubectl get pods --o wide
->to get pod details in yml/json format 
     kubectl get pods --o yaml
     kubectl get pods --o json
->to get the all details of pod
    kubectl describe pods pod-name
NOTE - imperative lo nkts, port,volumes ivvalemu jst pod cretae cheyyachu
  2.declarative
->realtime lo declarative tho pods create chestamu
->vim pod-name.yml
NOTE - 1.pod name can be anything in declartive but extension is imp
        2. to chk apiversion n kind of a pod using cmd kubectl api-resources(anni objects kitelustai)
    apiVersion : v1
    kind: Pod
    metadata: 
      name: pod-name
    spec:
      containers:
        - name: cont-name
          image: image-name
          ports:
            - containerPort: container-port
->example for nginx
    apiVersion : v1
    kind: Pod
    metadata: 
      name: pod2
    spec:
      containers:
        - name: mustafa
          image: shaikmustafa/dm
          ports:
            - containerPort: 80
NOTE - 1.while running manifest files, make sure v is capital in apiVersion, p is capital in Pod n p shd be capital in containerport
       2.image shd be frm docker-hub
       3.indentation shd be crt
->to run manifest file
    kubectl create -f file-name
    kubectl create -f pod.yml

    



RESUME AT hr







